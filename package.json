{
  "name": "eduai-gamification",
  "version": "1.0.0",
  "private": true,
  "description": "EduAI gamification prototype wrapped for Android via Capacitor",
  "scripts": {
    "prepare:www": "python scripts/prepare_www.py",
    "sync:android": "npm run prepare:www && npx cap sync android",
    "open:android": "npx cap open android",
    "build:android": "npm run sync:android && cd android && .\\gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.2.0",
    "@capacitor/core": "^7.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.2.0"
  }
}
